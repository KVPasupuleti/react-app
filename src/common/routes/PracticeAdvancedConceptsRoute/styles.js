import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const HOCHeader = styled.h1`${tw`text-3xl font-bold mt-8 mb-2 text-center`}`

const RenderPropsHeader = styled.h1`${tw`text-3xl font-bold text-center mt-4 mb-2`}`

export { HOCHeader, RenderPropsHeader }