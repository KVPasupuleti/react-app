import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const DisplayContainer = styled.div`${tw`flex flex-col bg-gray-300 items-center px-2 py-4`}`

const Header = styled.h2`${tw`text-xl font-bold`}`

const TextEl = styled.span`${tw``}`

export { DisplayContainer, Header, TextEl }