import styled from "@emotion/styled";
import tw from 'tailwind.macro'

const SizeFilterContainer = styled.div`${tw`w-1/4 px-4`}`

const SizesText = styled.p`${tw`my-4 font-bold`}`

export { SizeFilterContainer, SizesText } 